# مِسبارُ دفعة السكانر — `scan_probe.py`

مِسبارُ قياسٍ **قراءةٌ محضة** لدفعة السكانر في `quantum_matrix_v2.py`:
يفصل زمنَ الدفعة إلى شبكةٍ وحسابٍ وازدحامِ خيوطٍ وكلفةِ بطاقة،
ويقيس فائضَ تكرارِ الأطر. لا يُصدر أمراً، ولا يكتب
ملفّاً، ولا يمسّ السوق.

## التركيب — سطرٌ واحد

في `main()` بعد إطلاق الخيوط مباشرةً:

```python
import scan_probe; scan_probe.install(sys.modules[__name__])
```

والإزالة: احذف السطر (أو `scan_probe.uninstall(mod)` حيّاً).

## الفحوص — قبل التركيب

```bash
python tests/test_scan_probe.py        # 95/95 · ثوانٍ · لا شبكةَ ولا بوت
```

## المحتوى

```
scan_probe.py                      المِسبار
tests/test_scan_probe.py           95 فحصاً · على 3.10→3.13
docs/PROBE_AUDIT_2026-09-12.md     مراجعةُ المِسبار — أربعةُ أعطالٍ مقيسةٌ وعلاجُها
docs/BATCH_PROBE_2026-09-12.md     التحليلُ البنيويُّ لدفعة 49.6 دقيقة (كما وُرد)
docs/HANDOFF_2026-09-11.md         تسليمُ الجلسة السابقة (كما وُرد)
docs/PREREG_DECAY_qm-breakout-survivor.md
                                   سجلٌّ مُسبَقٌ لاختبار الضمور — كُتب قبل الرقم
```

## قبل أن تبني على رقم

اقرأ `docs/PROBE_AUDIT_2026-09-12.md` § ⑤ — فيه **ما لم يُقَس**، وفحصان
بسطرٍ واحدٍ على شجرتك يقرِّران هل تعريفُ «نهاية الدفعة» صحيحٌ في بوتك.
